{
  "name": "alexi-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "TINA_PUBLIC_IS_LOCAL=true tinacms dev -c \"eleventy --serve\"",
    "dev:network": "TINA_PUBLIC_IS_LOCAL=true tinacms dev -c \"eleventy --serve --host=0.0.0.0\"",
    "build": "tinacms build && eleventy",
    "build:prod": "TINA_BASE_PATH=alexi-website PATH_PREFIX=/alexi-website/ npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "tinacms": "^2.9.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@tinacms/cli": "^1.12.4",
    "@types/node": "^24.10.1"
  }
}
